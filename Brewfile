# RetroArch is NOT brew-installed because the cask is x86_64-only and runs
# under Rosetta on Apple Silicon. install.sh fetches the official universal
# build from libretro instead.
cask "dolphin"
cask "pcsx2"
cask "melonds"
cask "es-de"
brew "sevenzip"
